// @ts-nocheck
// The model catalog exposed to the loaders; already in core-auth ProviderModel shape, so it passes straight through.

export { OPENCODE_MODEL_DEFINITIONS as models } from "../plugin/config/models.js";
